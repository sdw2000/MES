# 小程序扫码领退料接口契约（Phase 1）

> 适用后端：MES（Spring Boot）
> 基础路径：`/api/stock/scan`
> 鉴权：需要登录 token（建议同时传 `token` 与 `X-Token`）

---

## 1) 扫码解析

### 请求
- Method: `GET`
- URL: `/api/stock/scan/resolve`
- Query:
	- `code` (string, required) 二维码/批次号/容器号
	- `stockType` (string, optional) `FILM` / `CHEMICAL`

### 成功响应示例（FILM）
```json
{
	"code": 20000,
	"msg": "查询成功",
	"data": {
		"stockType": "FILM",
		"detailId": 101,
		"stockId": 12,
		"qrCode": "ROLL-202605-0001",
		"batchNo": "20260501",
		"materialCode": "FM-100U",
		"materialName": "100um薄膜",
		"qty": 523.400,
		"unit": "m",
		"status": "available",
		"width": 1000
	}
}
```

---

## 2) 扫码领料

### 请求
- Method: `POST`
- URL: `/api/stock/scan/issue`
- Body (JSON):
	- `qrCode` (string, required)
	- `qty` (number, required)
	- `stockType` (string, optional)
	- `orderNo` (string, optional)
	- `scheduleId` (number, optional)
	- `processType` (string, optional)
	- `operator` (string, optional)
	- `deviceId` (string, optional)
	- `channel` (string, optional)
	- `idempotencyKey` (string, optional，强烈建议)
	- `remark` (string, optional)

### 请求示例（FILM，按长度 m）
```json
{
	"qrCode": "ROLL-202605-0001",
	"qty": 10,
	"stockType": "FILM",
	"orderNo": "SO20260504001",
	"scheduleId": 8899,
	"processType": "COATING",
	"operator": "miniapp_user_01",
	"deviceId": "wx-device-a1",
	"channel": "miniapp",
	"idempotencyKey": "issue-ROLL-202605-0001-20260504-001",
	"remark": "扫码领料"
}
```

### 成功响应示例
```json
{
	"code": 20000,
	"msg": "领料成功",
	"data": {
		"txnId": 10001,
		"txnNo": "MSTX20260504103000123123",
		"txnType": "ISSUE",
		"stockType": "FILM",
		"detailId": 101,
		"stockId": 12,
		"qrCode": "ROLL-202605-0001",
		"batchNo": "20260501",
		"materialCode": "FM-100U",
		"qty": 10.000,
		"unit": "m",
		"beforeQty": 523.400,
		"afterQty": 513.400,
		"sourceIssueTxnId": null,
		"orderNo": "SO20260504001",
		"scheduleId": 8899,
		"processType": "COATING",
		"createTime": "2026-05-04 10:30:00"
	}
}
```

---

## 3) 扫码退料

### 请求
- Method: `POST`
- URL: `/api/stock/scan/return`
- Body (JSON):
	- `qrCode` (string, required)
	- `sourceIssueTxnId` (number, required，必须关联原领料)
	- `qty` (number, required)
	- 其余上下文字段与领料相同（可选）

### 请求示例
```json
{
	"qrCode": "ROLL-202605-0001",
	"sourceIssueTxnId": 10001,
	"qty": 4,
	"operator": "miniapp_user_01",
	"channel": "miniapp",
	"idempotencyKey": "return-ROLL-202605-0001-20260504-001",
	"remark": "扫码退料"
}
```

### 成功响应示例
```json
{
	"code": 20000,
	"msg": "退料成功",
	"data": {
		"txnId": 10002,
		"txnNo": "MSTX20260504103100123123",
		"txnType": "RETURN",
		"stockType": "FILM",
		"detailId": 101,
		"stockId": 12,
		"qrCode": "ROLL-202605-0001",
		"batchNo": "20260501",
		"materialCode": "FM-100U",
		"qty": 4.000,
		"unit": "m",
		"beforeQty": 513.400,
		"afterQty": 517.400,
		"sourceIssueTxnId": 10001,
		"orderNo": "SO20260504001",
		"scheduleId": 8899,
		"processType": "COATING",
		"createTime": "2026-05-04 10:31:00"
	}
}
```

---

## 4) 事务流水分页

### 请求
- Method: `GET`
- URL: `/api/stock/scan/txn/list`
- Query:
	- `page` (int, default 1)
	- `size` (int, default 20)
	- `txnType` (optional: `ISSUE` / `RETURN`)
	- `stockType` (optional: `FILM` / `CHEMICAL`)
	- `orderNo` (optional)
	- `scheduleId` (optional)
	- `qrCode` (optional)

### 成功响应结构
```json
{
	"code": 20000,
	"msg": "查询成功",
	"data": {
		"records": [],
		"total": 0,
		"current": 1,
		"size": 20,
		"pages": 0
	}
}
```

---

## 5) 业务规则（已固化）

1. 薄膜 `FILM`：单位固定 `m`。
2. 化工 `CHEMICAL`：单位固定 `kg`。
3. 退料必须带 `sourceIssueTxnId`，并且必须是原领料事务。
4. 退料数量 `qty` 不得超过可退余额：
	 - 可退余额 = 原领料数量 - 已退总量
5. 退料二维码必须与原领料二维码一致（同一码回退）。
6. `idempotencyKey` 重复提交时，直接返回第一次成功结果（幂等）。

---

## 6) 典型错误码/错误信息约定

> 当前后端仍走统一 `ResponseResult.error(...)`，前端请以 `code != 200 && code != 20000` 判失败，并读取 `msg`。

常见失败 `msg`：
- `qrCode不能为空`
- `qty必须大于0`
- `sourceIssueTxnId不能为空`
- `sourceIssueTxnId无效，未找到原领料记录`
- `退料数量超过可退数量，可退=..., 本次=...`
- `退料二维码与原领料不一致，必须退回同一码`
- `未找到对应二维码库存: ...`

---

## 7) 小程序接入建议

1. 每次领/退料都生成唯一 `idempotencyKey`。
2. 页面保存最近一次成功返回的 `txnId`（用于退料时传 `sourceIssueTxnId`）。
3. 出错直接展示 `msg`。
4. 成功后刷新 `resolve` 与 `txn/list`，做前后余额校验。

