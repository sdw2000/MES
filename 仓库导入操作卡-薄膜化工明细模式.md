# 仓库导入操作卡（薄膜/化工明细模式）

> 适用范围：MES 库存导入导出（薄膜、化工）
> 目标：以明细为事实源，系统自动重算汇总，避免手工改汇总导致失真。

---

## 1. 总原则（先看）

1. **只维护明细，不手工维护汇总**。
2. **导入/导出都使用“明细模板”**。
3. 汇总库存由系统按明细自动计算。

---

## 2. 字段与去重规则

### 2.1 薄膜明细

- 关键字段：物料编号、批次号、卷号、厚度、宽度、长度、面积、状态。
- 文件内唯一键（去重键）：
  - **物料编号 + 批次号 + 卷号**

### 2.2 化工明细

- 关键字段：物料编号、批次号、桶号/包号、单位、重量、状态。
- 文件内唯一键（去重键）：
  - **物料编号 + 批次号 + 桶号/包号**

---

## 3. 标准操作流程（四步）

### 步骤 1：下载模板或导出当前明细

- 薄膜模板：`/api/stock/film/template`
- 薄膜明细导出：`/api/stock/film/export/import-format`
- 化工模板：`/api/stock/chemical/template`
- 化工明细导出：`/api/stock/chemical/export/import-format`

> 建议：优先“导出当前明细”后编辑，再回导。

### 步骤 2：在 Excel 里修改明细

- 保持表头不改名、不换列顺序（最安全）。
- 不要新增重复去重键。
- 数值字段只填数字（例如面积、重量）。

### 步骤 3：回导

- 薄膜导入：`/api/stock/film/import`
- 化工导入：`/api/stock/chemical/import`

### 步骤 4：核对结果

- 看导入返回：successCount、skipCount。
- 再看列表页汇总是否符合预期。

---

## 4. 常见错误与处理

1. **跳过数量（skipCount）> 0**
   - 处理：下载跳过明细，逐行修正后再次导入。

2. **重复主键**
   - 薄膜：同一 物料+批次+卷号 重复。
   - 化工：同一 物料+批次+桶号 重复。
   - 处理：保留一条，其他改卷号/桶号或删除。

3. **重量/面积为 0 或空**
   - 处理：补齐有效数值（>0），否则会导致汇总异常或被跳过。

4. **误用旧汇总模板**
   - 化工已强制明细模式，旧汇总模板会被拒绝。

---

## 5. 每日执行清单（仓库）

- [ ] 先导出当前明细备份
- [ ] 按明细改动（不改汇总）
- [ ] 检查去重键是否重复
- [ ] 导入并确认 successCount/skipCount
- [ ] 抽查 3~5 个料号，核对明细与汇总一致

---

## 6. 口径说明（对齐业务）

- **明细是事实源**：每卷/每桶可追溯。
- **汇总是计算结果**：仅用于展示与统计。
- 任何库存争议，优先回溯明细记录。