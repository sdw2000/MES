# 故障复盘：`api.maxtritape.com` 502 到恢复（2026-05-04）

## 1. 现象

- 小程序登录失败
- `https://api.maxtritape.com/user/login`、`/actuator/health` 返回 `502`
- 本地 MES 服务本身可用

## 2. 影响范围

- 小程序/前端通过固定域名访问后端全部受影响
- 生产可用性下降，登录链路中断

## 3. 根因

### 根因一：Nginx 配置重复加载
- `/etc/nginx/nginx.conf` 中 `include /etc/nginx/conf.d/*.conf;` 出现两次
- 导致同一 `server_name api.maxtritape.com` 被重复加载
- Nginx 报警：`conflicting server name ... ignored`

### 根因二：云端错误回源目标
- 回源历史曾指向无监听端口（如 `127.0.0.1:18090` 未就绪）或动态隧道地址变更
- 造成网关到上游不可达，最终表现 502

## 4. 处置过程

1. 验证本地 MES：`127.0.0.1:8090` 正常
2. 改为 FRP 固定端口方案：
   - 云端 `frps`（7000）
   - 本地 `frpc` 将 `8090 -> 云端 18090`
3. 云端验证：`curl http://127.0.0.1:18090/actuator/health` 返回 200
4. 修复 Nginx 主配置重复 include（删除一条）
5. `nginx -t` 通过，reload 后恢复固定域名可用

## 5. 验证结果

- `https://api.maxtritape.com/actuator/health`：`200`
- `POST /user/login`：`200 + 业务响应`（出现 401 代表鉴权逻辑生效，非网络故障）
- 502 问题消失

## 6. 最终架构

- 域名入口：`api.maxtritape.com`（阿里云 Nginx）
- 云端回源：`127.0.0.1:18090`
- 隧道：FRP 固定映射
- 本地服务：MES `127.0.0.1:8090`

## 7. 预防措施

1. `nginx.conf` 仅保留一条 `include /etc/nginx/conf.d/*.conf;`
2. `frps/nginx` 设置开机自启（云端）
3. `frpc/MES` 设置开机自启（本地）
4. 增加每日健康检查：
   - `https://api.maxtritape.com/actuator/health`
5. 规范告警分层：
   - `502/504` 归网络链路
   - `401` 归账号鉴权

## 8. 结论

本次故障已完成修复，固定域名链路恢复。后续主要风险点为“本地服务未自动启动”，可通过启动项治理基本消除。