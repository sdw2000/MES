# 包材仓（管芯/纸箱）数据定义与流程

## 1. 目标
- 新增独立仓别：**包材仓**。
- 管芯、纸箱、PE/PP/PVC 管材统一进入包材仓。
- 与薄膜仓、化工仓口径分离，避免同料号双仓重复。

## 2. 数据模型（研发定义）

### 2.1 主表：`package_stock`
- 粒度：按 `material_code` 聚合。
- 关键字段：
  - `material_code`, `material_name`, `spec_desc`, `unit`
  - `total_quantity`, `available_quantity`, `locked_quantity`
  - `total_pack_count`, `available_pack_count`, `locked_pack_count`
  - `status`（`active/low_stock/out_of_stock`）

### 2.2 明细表：`package_stock_detail`
- 粒度：入库批次明细。
- 关键字段：
  - `stock_id`（关联 `package_stock.id`）
  - `material_code`, `batch_no`, `container_no`
  - `pack_uom`, `pack_count`, `std_uom=PCS`, `std_qty_per_pack=1`
  - `quantity`（按 PCS 计）
  - `warehouse=包材仓`, `location`, `supplier`, `storage_date`
  - `status`（`available/locked/used`）

### 2.3 出库表：`package_stock_out`
- 粒度：出库操作记录。
- 关键字段：
  - `stock_id`, `detail_id`, `material_code`, `out_no`
  - `out_quantity`, `out_type`, `operator`, `out_date`

## 3. 分类规则
- 统一分类值：`PACKAGING`。
- 判定命中（任一）：
  - 料号/名称/规格含 `PEG`、`PE管`、`PP管`、`PVC管`、`纸管`、`纸箱`、`TUBE`、`管材`
  - 采购单位为 `个/支/箱`

## 4. 入库流程

### 4.1 采购收货入库（已落地）
1. 采购收货生成 `TapeInboundRequest`。
2. 分类函数返回 `PACKAGING`。
3. 入库分流进入 `inboundToPackageWarehouse()`。
4. 写入 `package_stock_detail`（按批次聚合单条，`pack_count` 记录数量）。
5. 刷新 `package_stock` 汇总。
6. 记录 `stock_flow_log.stock_type=PACKAGE` 的 IN 流水。

### 4.2 其他入库
- 盘点调整入库：建议统一通过包材仓明细新增，并触发汇总刷新。
- 退料入库：建议写入 `package_stock_detail`，状态 `available`。

## 5. 出库流程

### 5.1 领料出库（建议实现口径）
1. 选定 `package_stock_detail` 可用明细。
2. 先锁定（`status=locked`）。
3. 审核通过后出库（`status=used`）。
4. 写入 `package_stock_out`。
5. 刷新 `package_stock`。
6. 记录 `stock_flow_log` OUT 流水。

### 5.2 取消/回滚
- 已锁定未出库：`locked -> available`。
- 已出库冲销：新增反向记录（不建议物理删除）。

## 6. 本次已完成项
1. 新增包材仓数据表与索引。
2. 新增包材仓 Java 实体/Mapper。
3. 采购分类口径切换到 `PACKAGING`。
4. 入库分流新增 `inboundToPackageWarehouse()`。
5. 历史数据迁移脚本（含明细迁移、汇总回写）。
6. 来源仓软清理脚本（去重，避免双仓重复）。

## 7. 执行脚本
- 建仓+迁移：`sql/patch/2026-05-12-create-package-warehouse-and-migrate.sql`
- 来源仓去重：`sql/patch/2026-05-12-package-warehouse-cleanup-source.sql`

## 8. 验证口径
- `package_stock` 与 `package_stock_detail` 汇总一致。
- `film_stock`/`chemical_stock` 对应包材料号库存归零。
- 新采购入库 token 与分流均为 `PACKAGING`。
