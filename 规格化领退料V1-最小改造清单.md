# 规格化领退料 V1 最小改造清单（按采购规格/包装数领退）

## 0. 目标口径（冻结）

1. 领退动作统一按“最小包装数量”执行（桶/包/卷），数量必须为整数。
2. 每个包装绑定标准换算量：`std_qty_per_pack`（kg/桶、kg/包、m/卷、㎡/卷）。
3. 化工、薄膜统一流程：入库按采购规格写入；领料/退料按包装数；报表按标准量折算。

---

## 1. 数据库改造（V1 必做）

### 1.1 明细表补字段（事实源）

- `chemical_stock_detail`
  - `pack_uom` VARCHAR(20) NULL COMMENT '最小包装单位：桶/包'
  - `pack_count` INT NULL DEFAULT 1 COMMENT '当前包装数量（整数）'
  - `std_uom` VARCHAR(20) NULL COMMENT '标准单位：kg'
  - `std_qty_per_pack` DECIMAL(18,6) NULL COMMENT '每包装标准量'
- `film_stock_detail`
  - `pack_uom` VARCHAR(20) NULL COMMENT '最小包装单位：卷'
  - `pack_count` INT NULL DEFAULT 1 COMMENT '当前包装数量（整数）'
  - `std_uom` VARCHAR(20) NULL COMMENT '标准单位：m/㎡'
  - `std_qty_per_pack` DECIMAL(18,6) NULL COMMENT '每包装标准量'

### 1.2 扫码流水补字段

- `material_scan_txn`
  - `pack_qty` INT NULL COMMENT '领退包装数量（整数）'
  - `pack_uom` VARCHAR(20) NULL COMMENT '包装单位'
  - `std_qty` DECIMAL(18,6) NULL COMMENT '折算标准量'
  - `std_uom` VARCHAR(20) NULL COMMENT '标准单位'

### 1.3 汇总表补字段（便于前端展示）

- `chemical_stock`
  - `available_pack_count` INT NULL
  - `locked_pack_count` INT NULL
  - `total_pack_count` INT NULL
- `film_stock`
  - `available_pack_count` INT NULL
  - `locked_pack_count` INT NULL
  - `total_pack_count` INT NULL

### 1.4 迁移脚本

新增迁移脚本（建议）：
- `src/main/resources/db/migration/alter-chemical-stock-detail-add-pack-fields.sql`
- `src/main/resources/db/migration/alter-film-stock-detail-add-pack-fields.sql`
- `src/main/resources/db/migration/alter-material-scan-txn-add-pack-fields.sql`
- `src/main/resources/db/migration/alter-stock-summary-add-pack-counts.sql`

---

## 2. 后端模型改造（V1 必做）

### 2.1 实体字段

- `ChemicalStockDetail` 增加：`packUom`, `packCount`, `stdUom`, `stdQtyPerPack`
- `FilmStockDetail` 增加：`packUom`, `packCount`, `stdUom`, `stdQtyPerPack`
- `MaterialScanTxn` 增加：`packQty`, `packUom`, `stdQty`, `stdUom`
- `ChemicalStock` / `FilmStock` 增加包装汇总字段

---

## 3. 采购入库归一化（V1 核心）

入口：`PurchaseReceiptServiceImpl`

在以下节点落地：
- `syncInboundRequestsFromReceipt()`
- `upsertInboundRequestByReceiptItem()`
- `resolveInboundCategory()`

新增规则函数（建议）：
- `resolvePackMeta(PurchaseReceiptItem item, PurchaseOrderItem poItem)`
  - 输出：`packUom`, `packQty`, `stdUom`, `stdQtyPerPack`

规则：
1. `packQty` 取采购“桶数/卷数/包数”字段（若缺失，按 1 兜底并打风险标记）。
2. `stdQtyPerPack = stockQty / packQty`（优先），失败则尝试 `conversionRate`。
3. 化工默认 `stdUom=kg`；薄膜默认 `stdUom=m`（可按已有面积策略切换 `㎡`，但全链路保持唯一）。
4. 将归一化结果写入入库备注 token（用于审批链路透传），并最终写入 detail 字段。

---

## 4. 库存服务改造（V1 必做）

### 4.1 化工库存

入口：`ChemicalStockServiceImpl`
- `createDetail()` / `updateDetail()`：落 pack 字段校验。
- `importExcel()`：模板扩展并支持 pack 字段导入。
- 汇总重算：按 `pack_count` 统计包装数；按 `pack_count * std_qty_per_pack` 回算重量。

### 4.2 薄膜库存

入口：`FilmStockServiceImpl`
- `createDetail()` / `updateDetail()`：落 pack 字段校验。
- `importExcel()`：模板扩展并支持 pack 字段导入。
- 汇总重算：按 `pack_count` 统计包装数；标准量由 `pack_count * std_qty_per_pack`。

---

## 5. 扫码领退料改造（V1 核心）

入口：`MaterialScanServiceImpl`

### 5.1 接口入参

- `/api/stock/scan/issue`
  - 新增必填：`packQty`（整数）
  - 保留兼容：`qty`（短期，仅兼容）
- `/api/stock/scan/return`
  - 新增必填：`packQty`（整数）
  - 保留兼容：`qty`（短期，仅兼容）

### 5.2 核心逻辑

- `issueByScan()` / `returnByScan()`：优先处理 `packQty`
- 化工：
  - 可领条件：`packCount >= packQty`
  - 扣减：`packCount -= packQty`
  - `stdQty = packQty * stdQtyPerPack` 同步写流水
- 薄膜：
  - 可领条件：`packCount >= packQty`
  - 扣减：`packCount -= packQty`
  - `stdQty = packQty * stdQtyPerPack` 同步写流水
- 退料：同理反向，且不超过原领 `packQty - 已退packQty`

### 5.3 流水字段

在 `buildBaseTxn()` 或 `issue*/return*` 分支里写：
- `packQty`
- `packUom`
- `stdQty`
- `stdUom`

---

## 6. 锁库与排程口径同步（V1 必做）

现有 lock-query/锁定逻辑增加一步换算：

- `requiredPack = ceil(requiredStdQty / stdQtyPerPack)`
- 锁定按包装数执行，不允许小数包装。

适配位置：库存锁定与排程相关服务中所有“按重量/长度直接锁定”入口。

---

## 7. 历史数据归一化（LMR-1819-3 同类）

新增一次性任务（建议 admin 接口 + SQL 批处理）：
1. 按采购收货记录回填 detail 的 `pack_uom/std_qty_per_pack/pack_count`。
2. 无法推导的记录标记 `remark` 风险标签（如 `packMetaMissing=1`），禁止领料。
3. 执行汇总重算并落地 `available/locked/total_pack_count`。
4. 产出审计报表：成功数/跳过数/人工处理清单。

---

## 8. 接口与前端联动（ERP/小程序）

### 8.1 API 变更

- 扫码解析返回新增：
  - `packUom`, `packCount`, `stdUom`, `stdQtyPerPack`
- 领退接口请求新增：
  - `packQty`（必填）

### 8.2 前端交互

- 输入框从“kg/m”改为“桶/包/卷”。
- 显示“可用包装数”和“折算标准量”。
- 禁止小数输入。

---

## 9. 上线分期（建议）

### Phase A（1 周）
- 后端双写双读：支持 `packQty` + 兼容旧 `qty`。
- 完成字段迁移、实体改造、服务重算。

### Phase B（1 周）
- 前端全切 `packQty`。
- 旧 `qty` 入参告警并逐步下线。

### Phase C（3 天）
- 清理旧口径分支。
- 发布最终口径说明。

---

## 10. 验收标准（必须全部通过）

1. 领料/退料仅接受整数包装数。
2. 不允许直接领 `0.5桶`、`3.2kg`、`2.7m`。
3. 退料不超过原领包装数。
4. 明细包装数与汇总包装数一致。
5. 折算标准量只用于成本/报表，不影响领退动作语义。
6. LMR-1819-3 用例按“包装数领退”完整通过。
